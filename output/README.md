# Output Logs

Simulation logs are generated automatically by running:

./run.sh 100

or, for the default full duration:

./run.sh

CSV log files are not tracked in Git because they are large generated files.
